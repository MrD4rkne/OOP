package Simulation;

import Collection.IMyList;
import Collection.MyArrayList;

public class Tram extends Vehicle {
    public Tram(Line line, int sideNo, int vehicleLineNo, int capacity) {
        super(line,sideNo,vehicleLineNo,capacity);
    }
}
